type MetaWarden @entity {
    id: ID!

    tokenID: BigInt!
    owner: Owner!
}

type Owner @entity {
    id: ID!

    tokens: [MetaWarden!]! @derivedFrom(field: "owner")
    assetCount: BigInt!
}

type Stat @entity {
    id: ID!

    assetCount: BigInt!
    ownerCount: BigInt!
}
